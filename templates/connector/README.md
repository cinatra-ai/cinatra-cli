# {{displayName}}

{{description}} This connector advertises a capability the host can route to, registered through its `register(ctx)` server entry. Its setup page is a declarative `cinatra.configSchema` (the hot-installable schema-config UI surface), not a bundled React page, so the host renders it without rebuilding.

## Works with

- The Cinatra host capability registry
- The declarative schema-config setup surface (no bundled React)

## Capabilities

- Describe the first capability this connector provides
- Describe a second capability, if any
- Replace these bullets with the real provider/tool surface
